create a new art-tools branch from prod/main and add all the pipelines and the resources that we created in this seession to ArgoCD

---

<attached_files>

<code_selection path="/Users/asdas/.cursor/plans/add_tekton_resources_to_argocd_c66ed8ec.plan.md" lines="12-14">
    12|CronJob: schedule-layered-products-scan-cron (suspended)
</code_selection>

</attached_files>

@add_tekton_resources_to_argocd_c66ed8ec.plan.md (12-14) why

---

ahh no - that right let it be that

---

what about Tekton Results loki config and the rest?

---

<attached_files>

<code_selection path="/Users/asdas/.cursor/plans/add_tekton_resources_to_argocd_c66ed8ec.plan.md" lines="1-50">
     1|# Add Tekton Resources to ArgoCD
     2|
     3|## Resources to Add
     4|
     5|From the `artc2023` cluster (`art-cd` namespace), the following were created during this session:
     6|
     7|- **Task**: `artcd` (shared by all pipelines)
     8|- **Pipelines** (6 total):
     9|  - `olm-bundle-konflux`
    10|  - `build-fbc`
    11|  - `build-layered-products`
    12|  - `layered-products-scan`
    13|  - `release-from-fbc`
    14|  - `schedule-layered-products-scan`
    15|- **CronJob**: `schedule-layered-products-scan-cron` (suspended)
    16|
    17|## Directory Layout
    18|
    19|Following the existing convention in `art-cluster/pipelines/config/argocd/project/art-cd/` where resources are grouped by feature:
    20|
    21|```
    22|art-cluster/pipelines/config/argocd/project/art-cd/
    23|  common/
    24|    artcd-task.yaml                              # NEW - shared Task
    25|    image/...                                    # existing
    26|    secrets/...                                  # existing
    27|  layered-products/                              # NEW directory
    28|    olm-bundle-konflux-pipeline.yaml
    29|    build-fbc-pipeline.yaml
    30|    build-layered-products-pipeline.yaml
    31|    layered-products-scan-pipeline.yaml
    32|    release-from-fbc-pipeline.yaml
    33|    schedule-layered-products-scan-pipeline.yaml
    34|    schedule-layered-products-scan-cronjob.yaml
    35|```
    36|
    37|The `artcd` Task goes under `common/` since it is shared across all pipelines (same pattern as `common/image/` and `common/secrets/`). All layered product Pipelines and the scheduler CronJob go under a single `layered-products/` directory.
    38|
    39|## Steps
    40|
    41|1. Create a new branch from `prod/main` (e.g. `add-tekton-resources-to-argocd`)
    42|2. Export each resource from the cluster (stripping server-managed metadata like `creationTimestamp`, `resourceVersion`, `uid`, `generation`, `managedFields`)
    43|3. Write clean YAML files to the directory structure above
    44|4. Move the existing `layered-products-scan/cronjob.yaml` into `layered-products/schedule-layered-products-scan-cronjob.yaml` and remove the old directory
    45|5. Push to origin and create a PR against `prod/main`
    46|
    47|## Note on Containerfile.base
    48|
    49|The Containerfile.base changes (oras, opm, cosign installs) are already merged to `prod/main` via PRs #2674 and #2676, so they are not included in this branch.
    50|
</code_selection>

</attached_files>

Implement the plan as specified, it is attached for your reference. Do NOT edit the plan file itself.

To-do's from the plan have already been created. Do not create them again. Mark them as in_progress as you work, starting with the first one. Don't stop until you have completed all the to-dos.

---

what about the tasks https://console-openshift-console.apps.artc2023.pc3z.p1.openshiftapps.com/tasks/ns/art-cd

---

also no --no-verify

---

i already update the base image for the oras-cli - we can remove it from the release from FBC task -