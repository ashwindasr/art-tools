use the jenkins MCP server to look at this error https://art-jenkins.apps.prod-stable-spoke1-dc-iad2.itup.redhat.com/job/aos-cd-builds/job/build%252Flayered-products/1792/console  - the image should be disabled so the layered-oeprator scan should have skipped the build trigger - look at ocp4-scan-konflux for reference

---

use the jenkins MCP server to look at this error https://art-jenkins.apps.prod-stable-spoke1-dc-iad2.itup.redhat.com/job/aos-cd-builds/job/build%252Flayered-products/1792/console  - the image should be disabled so the layered-oeprator scan should have skipped the build trigger - look at ocp4-scan-konflux for reference

---

Implement the plan as specified, it is attached for your reference. Do NOT edit the plan file itself.

To-do's from the plan have already been created. Do not create them again. Mark them as in_progress as you work, starting with the first one. Don't stop until you have completed all the to-dos.

---

Implement the plan as specified, it is attached for your reference. Do NOT edit the plan file itself.

To-do's from the plan have already been created. Do not create them again. Mark them as in_progress as you work, starting with the first one. Don't stop until you have completed all the to-dos.

---

create a new branch commit push to origin and raise a PR to prod

---

create a new branch commit push to origin and raise a PR to prod

---

im confused - ocp4-scan-konflux should already have been smart? why are we chaning things there?

---

yes

---

yes

---

Implement the plan as specified, it is attached for your reference. Do NOT edit the plan file itself.

To-do's from the plan have already been created. Do not create them again. Mark them as in_progress as you work, starting with the first one. Don't stop until you have completed all the to-dos.

---

Implement the plan as specified, it is attached for your reference. Do NOT edit the plan file itself.

To-do's from the plan have already been created. Do not create them again. Mark them as in_progress as you work, starting with the first one. Don't stop until you have completed all the to-dos.

---

update the PR description as well

---

update the PR description as well

---

add this only for non openshift groups

---

add this only for non openshift groups

---

update PR description

---

update PR description

---

Verify each finding against the current code and only fix it if needed.

In `@doozer/doozerlib/cli/scan_sources_konflux.py` around lines 1533 - 1537, The
loop that marks images unbuildable by checking img.data.get('mode') is too
strict; instead reuse the runtime's loadability rules (as implemented in
late_resolve_image) so images with okd.mode or load_disabled behavior remain
loadable. Replace the simple mode check in the loop over all_image_data with a
call that mirrors runtime.late_resolve_image(..., required=False) (or the
runtime flag/utility that evaluates load_disabled and okd.mode) to decide
buildability, and then set/clear has_disabled_dependency based on that result
rather than the raw top-level mode field.

---

Verify each finding against the current code and only fix it if needed.

In `@doozer/doozerlib/cli/scan_sources_konflux.py` around lines 1533 - 1537, The
loop that marks images unbuildable by checking img.data.get('mode') is too
strict; instead reuse the runtime's loadability rules (as implemented in
late_resolve_image) so images with okd.mode or load_disabled behavior remain
loadable. Replace the simple mode check in the loop over all_image_data with a
call that mirrors runtime.late_resolve_image(..., required=False) (or the
runtime flag/utility that evaluates load_disabled and okd.mode) to decide
buildability, and then set/clear has_disabled_dependency based on that result
rather than the raw top-level mode field.

---

reply back to say why it doesn't apply here https://github.com/openshift-eng/art-tools/pull/2710#discussion_r3033424939

---

reply back to say why it doesn't apply here https://github.com/openshift-eng/art-tools/pull/2710#discussion_r3033424939

---

https://art-jenkins.apps.prod-stable-spoke1-dc-iad2.itup.redhat.com/job/aos-cd-builds/job/build%252Flayered-products/1807/console - I'm still seeing an error here. How are we doing it in OCP4-konflux?

---

https://art-jenkins.apps.prod-stable-spoke1-dc-iad2.itup.redhat.com/job/aos-cd-builds/job/build%252Flayered-products/1807/console - I'm still seeing an error here. How are we doing it in OCP4-konflux?

---

Yes, let's do that, and let's just focus on the layered products. The PR is already merged. Create a new branch from prod main, then commit and push to origin, and raise a PR to prod.

---

Yes, let's do that, and let's just focus on the layered products. The PR is already merged. Create a new branch from prod main, then commit and push to origin, and raise a PR to prod.